@model IEnumerable<BeyondTheTutor.Models.TimeSheetModels.Day>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TimeSheet.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.On)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegularHrs)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TimeSheet.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.On)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegularHrs)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
