@model IEnumerable<BeyondTheTutor.Models.TutorSchedule>

<div class="ui green message">Schedule Successfully Added!</div>
<table class="ui striped table">
    <thead>
        <tr>
            <th><h4>Tutor Name &amp; Job</h4></th>
            <th> <h4>Start Date &amp; Time</h4></th>
            <th>End Date &amp; Time</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>@Html.DisplayFor(modelItem => item.StartTime)</td>
                <td>@Html.DisplayFor(modelItem => item.EndTime)</td>
            </tr>
        }
    </tbody>
</table>
<a href="/Tutor/TutorSchedules/Create" button class="fluid ui button schedule" style="color: rgb(196,198,200);">Add More Schedules</a>



